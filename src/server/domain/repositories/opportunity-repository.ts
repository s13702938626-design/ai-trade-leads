import type { Activity, Contact, FollowUpTask, Opportunity, OpportunityScoreRevision, OutreachDraft, ProductLine, Signal } from "../entities/opportunity";
import type { PageOptions } from "./account-repository";
export interface OpportunityRepository { listProductLines(): Promise<ProductLine[]>; createProductLine(input: Omit<ProductLine, "id" | "createdAt" | "updatedAt">): Promise<ProductLine>; createSignal(input: Omit<Signal, "id" | "createdAt" | "updatedAt">): Promise<Signal>; listSignals(accountId?: string): Promise<Signal[]>; upsertOpportunity(input: Omit<Opportunity, "id" | "createdAt" | "updatedAt" | "deletedAt">): Promise<Opportunity>; getOpportunity(id: string): Promise<Opportunity | null>; listOpportunities(options?: PageOptions & { priority?: Opportunity["priority"] }): Promise<Opportunity[]>; createScoreRevision(input: Omit<OpportunityScoreRevision, "id" | "createdAt">): Promise<OpportunityScoreRevision>; listScoreRevisions(opportunityId: string): Promise<OpportunityScoreRevision[]>; createContact(input: Omit<Contact, "id" | "createdAt" | "updatedAt" | "deletedAt">): Promise<Contact>; createActivity(input: Omit<Activity, "id" | "createdAt">): Promise<Activity>; listActivities(accountId?: string, opportunityId?: string): Promise<Activity[]>; createTask(input: Omit<FollowUpTask, "id" | "createdAt" | "updatedAt" | "completedAt">): Promise<FollowUpTask>; listTasks(options?: { status?: FollowUpTask["status"] }): Promise<FollowUpTask[]>; completeTask(id: string): Promise<FollowUpTask>; createDraft(input: Omit<OutreachDraft, "id" | "createdAt" | "updatedAt" | "deletedAt">): Promise<OutreachDraft>; listDrafts(accountId?: string): Promise<OutreachDraft[]>; }
