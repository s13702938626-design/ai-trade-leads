import type { AccountId, EvidenceId, EvidenceResolutionStatus, EvidenceSourceType, SourceRunId } from "./common";
export type Evidence = { id: EvidenceId; sourceType: EvidenceSourceType; sourceExternalId: string | null; sourceUrl: string | null; canonicalUrl: string | null; title: string | null; rawText: string | null; excerpt: string | null; publishedAt: Date | null; observedAt: Date; fetchedAt: Date; contentHash: string; sourceRunId: SourceRunId; accountId: AccountId | null; resolutionStatus: EvidenceResolutionStatus; metadata: Record<string, unknown>; legacyPayload: unknown | null; createdAt: Date; updatedAt: Date; deletedAt: Date | null };
export type EvidenceCreateResult = { status: "created"; evidence: Evidence } | { status: "duplicate"; evidence: Evidence; duplicateReason: "source_external_id" | "canonical_url_content_hash" };
