import type { EvidenceSourceType } from "../../domain/entities/common";
export function mapLegacySourceType(value: unknown): { sourceType: EvidenceSourceType; warning: string | null } { const raw = typeof value === "string" ? value.trim().toLowerCase() : ""; const map: Record<string, EvidenceSourceType> = { customs_csv: "customs_csv", web_search: "web_search", serper: "web_search", google: "web_search", bing: "web_search", tender: "tender", rfq_platform: "rfq_platform", demand_signal_search: "rfq_platform", marketplace: "marketplace", company_website: "company_website", news: "news", forum: "forum", manual_url: "manual_url", manual_public_web: "manual_url" }; return { sourceType: map[raw] ?? "manual_url", warning: map[raw] ? null : "unknown_legacy_source_type" }; }
