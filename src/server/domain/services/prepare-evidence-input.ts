import type { Clock } from "../entities/common";
import { buildEvidenceContentHash, canonicalizePublicUrl, stableJsonStringify } from "../normalization";
export function prepareEvidenceCreateInput(input: { sourceType: string; sourceExternalId?: string | null; sourceUrl?: string | null; title?: string | null; rawText?: string | null; excerpt?: string | null; publishedAt?: Date | null; observedAt?: Date; fetchedAt?: Date; metadata?: Record<string, unknown>; legacyPayload?: unknown | null }, clock: Clock) { const now = clock.now(); const canonicalUrl = canonicalizePublicUrl(input.sourceUrl); const metadata = input.metadata ?? {}; const publishedAt = input.publishedAt ?? null; const observedAt = input.observedAt ?? now; const fetchedAt = input.fetchedAt ?? now; for (const value of [publishedAt, observedAt, fetchedAt]) if (value && Number.isNaN(value.getTime())) throw new Error("Invalid evidence date."); return { ...input, sourceExternalId: input.sourceExternalId?.trim() || null, sourceUrl: input.sourceUrl?.trim() || null, canonicalUrl, title: input.title?.trim() || null, rawText: input.rawText?.trim() || null, excerpt: input.excerpt?.trim() || null, publishedAt, observedAt, fetchedAt, metadata, metadataJson: stableJsonStringify(metadata), legacyPayloadJson: input.legacyPayload == null ? null : stableJsonStringify(input.legacyPayload), contentHash: buildEvidenceContentHash({ sourceType: input.sourceType, sourceExternalId: input.sourceExternalId?.trim() || null, canonicalUrl, title: input.title?.trim() || null, rawText: input.rawText?.trim() || null, excerpt: input.excerpt?.trim() || null, publishedAt, metadata }) }; }
