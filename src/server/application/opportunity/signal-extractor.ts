import type { Evidence } from "../../domain/entities/evidence";
import type { Signal } from "../../domain/entities/opportunity";
const patterns: Array<[RegExp, Signal["signalType"], Signal["strength"]]> = [[/rfq|request for quotation|looking for supplier/i, "recent_rfq", "strong"], [/tender|procurement|bid/i, "recent_tender", "strong"], [/bulk purchase|bulk order/i, "bulk_purchase_request", "strong"], [/import/i, "recent_import", "strong"], [/distributor/i, "distributor_listing", "medium"], [/expansion|new product/i, "expansion", "medium"], [/manufacturer|factory/i, "target_customer_type_match", "weak"]];
export function extractSignalsFromEvidence(evidence: Evidence, productLineId: string, detectedAt = new Date()): Omit<Signal, "id" | "createdAt" | "updatedAt">[] { const text = [evidence.title, evidence.rawText, evidence.excerpt].filter(Boolean).join(" "); return patterns.filter(([pattern]) => pattern.test(text)).map(([, signalType, strength]) => ({ accountId: evidence.accountId!, productLineId, signalType, strength, confidence: "medium" as const, occurredAt: evidence.publishedAt, detectedAt, expiresAt: evidence.publishedAt ? new Date(evidence.publishedAt.getTime() + 90 * 86_400_000) : null, status: "active" as const, evidenceIds: [evidence.id], reasons: [`Evidence ${evidence.id} matched ${signalType}`], ruleVersion: "v1", supersededBySignalId: null })).filter(() => evidence.accountId !== null); }
