"use client";
import { useState } from "react";
export default function SourcesPage() { const [url, setUrl] = useState(""); const [message, setMessage] = useState("仅导入真实公开来源；SourceRun 只会创建 Evidence。"); const submit = async () => { const response = await fetch("/api/v1/sources/manual", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ url }) }); const data = await response.json() as { error?: string }; setMessage(response.ok ? "来源已记录为待核实 Evidence。" : data.error ?? "来源录入失败。"); }; return <main className="mx-auto max-w-6xl p-6"><h1 className="text-2xl font-semibold">高级数据来源</h1><p className="mt-2 text-sm text-slate-600">适配器只产生 Evidence，不会自动保存客户或机会。</p><section className="mt-6 max-w-xl border border-slate-200 bg-white p-5"><h2 className="font-medium">手工 URL Evidence</h2><input className="mt-3 w-full border border-slate-300 px-3 py-2 text-sm" placeholder="https://公开来源链接" value={url} onChange={(event) => setUrl(event.target.value)} /><button className="mt-3 rounded bg-slate-950 px-4 py-2 text-sm text-white" onClick={() => void submit()}>保存为待核实证据</button><p className="mt-3 text-sm text-slate-600">{message}</p></section></main>; }
